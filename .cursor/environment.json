{
  "$schema": "https://cursor.sh/schemas/environment.json",
  "commands": {
    "pytest_quiet": { "run": "pytest -q", "confirm": true },
    "ruff_fix": { "run": "ruff check --fix .", "confirm": true }
  }
}